<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.navercorp.pinpoint.alarm.dao.AlarmHistoryV2Dao">

    <insert id="insert"
            parameterType="com.navercorp.pinpoint.alarm.vo.AlarmHistoryV2"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO alarm_history_v2 (rule_id, event_type, message, context, created_at)
        VALUES (#{ruleId}, #{eventType}, #{message}, #{context}, UTC_TIMESTAMP())
    </insert>

    <update id="updateContext">
        UPDATE alarm_history_v2
        SET context = #{context}
        WHERE id = #{id}
    </update>

    <select id="selectById" parameterType="Long"
            resultType="com.navercorp.pinpoint.alarm.vo.AlarmHistoryV2">
        SELECT id, rule_id, event_type, message, context, created_at
        FROM alarm_history_v2
        WHERE id = #{id}
    </select>

    <select id="selectByIdForUpdate" parameterType="Long"
            resultType="com.navercorp.pinpoint.alarm.vo.AlarmHistoryV2">
        SELECT id, rule_id, event_type, message, context, created_at
        FROM alarm_history_v2
        WHERE id = #{id}
        FOR UPDATE
    </select>

    <select id="selectByRuleId"
            resultType="com.navercorp.pinpoint.alarm.vo.AlarmHistoryV2">
        SELECT
            id,
            rule_id,
            event_type,
            message,
            context,
            created_at
        FROM alarm_history_v2
        WHERE rule_id = #{ruleId}
        ORDER BY id DESC
        LIMIT #{limit}
    </select>

    <delete id="deleteByRuleId" parameterType="Long">
        DELETE FROM alarm_history_v2 WHERE rule_id = #{ruleId}
    </delete>

    <delete id="deleteByRuleIds">
        DELETE FROM alarm_history_v2
        WHERE rule_id IN
        <foreach item="ruleId" collection="ruleIds" open="(" separator="," close=")">
            #{ruleId}
        </foreach>
    </delete>

    <delete id="deleteOlderThan">
        <![CDATA[
        DELETE FROM alarm_history_v2
        WHERE created_at < #{threshold}
        LIMIT #{limit}
        ]]>
    </delete>

</mapper>
