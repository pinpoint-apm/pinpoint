import styled from '@emotion/styled';
import { StyleFlexVCentered } from './styles';

export const StyledMainHeader = styled.div`
  ${StyleFlexVCentered};
  width: 100%;
  height: 50px;
  padding: 0 15px;
  border-bottom: 1px solid var(--border-primary);
`