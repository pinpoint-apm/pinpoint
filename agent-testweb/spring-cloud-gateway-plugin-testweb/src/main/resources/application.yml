# Defined in commandlineArgument of agent-test pom.xml

server:
  port: 18080

logging:
  level:
    root: info

springdoc:
  swagger-ui:
    path: /

spring:
  redis:
    cluster:
      nodes:
        - localhost:18001
        - localhost:18002
        - localhost:18003
