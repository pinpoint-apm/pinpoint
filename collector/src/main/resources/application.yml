spring:
  main:
    allow-bean-definition-overriding: false
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
      use-suffix-pattern: true
  profiles:
    active: release

server:
  port: 8081

pinpoint.modules.collector.grpc.ssl.enabled: false
