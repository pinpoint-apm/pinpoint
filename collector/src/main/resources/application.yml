spring:
  profiles:
    active: release

server:
  port: 8081

pinpoint:
  modules:
    collector:
      grpc:
        ssl:
          enabled: false
    realtime:
      enabled: false

collector.receiver.grpc.span.stream.keepalive.timeout: -1
collector.receiver.grpc.stat.streamkeepalive.timeout: -1