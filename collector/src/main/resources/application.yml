spring:
  profiles:
    active: release

server:
  port: 8081

management:
  otlp:
    metrics:
      export:
        enabled: false

pinpoint:
  modules:
    collector:
      grpc:
        ssl:
          enabled: false
    realtime:
      enabled: false